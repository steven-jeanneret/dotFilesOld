The read-only <code><strong>XMLHttpRequest.status</strong></code> property returns the numerical status code of the response of the <code>XMLHttpRequest</code>. <code>status</code> will be an <code>unsigned short</code>. Before the request is complete, the value of <code>status</code> will be <code>0</code>. It is worth noting that browsers report a status of 0 in case of XMLHttpRequest errors too.